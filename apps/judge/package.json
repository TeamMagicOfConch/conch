{
  "name": "@conch/judge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup netlify/functions/judge.ts",
    "dev": "echo 'Note: netlify dev is currently disabled due to ESM/CommonJS compatibility issues. Use pnpm build && pnpm test instead.'",
    "lint": "eslint \"**/*.ts*\"",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf netlify/functions dist"
  },
  "dependencies": {
    "@netlify/functions": "^2.7.0",
    "@toon-format/toon": "^1.0.0",
    "openai": "^4.70.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "express": "^4.18.2",
    "tsup": "^8.0.1",
    "tsx": "^4.7.0"
  }
}

